var vel = 59.5;
if (vel > 60) {
  console.log("Você ultrapassou a velocidade permitida, MULTADO(A)!");
} else {
  console.log(
    `A velocidade do seu carro é ${vel}, use cinto de segurança e dirija com cuidado.`
  );
}
